//import java.util.Scanner;

public class poly_main {
    public static void main(String[] args)
    {
  
    /* 
        Scanner scanner = new Scanner(System.in);
        System.out.println("database tipi [1 (MsSQL), 2 (MySQL)]: ");
        int dbType = scanner.nextInt();
        scanner.close();

        polymorphism dbConnection = null;

        switch(dbType){

            case 1:
                dbConnection = new polymorphism2();
                break;
            case 2:
                dbConnection = new polymorphism3();
                break;

            default:
                System.out.println("Yanlış seçim");
                break;
        }

        if(dbConnection != null){
            dbConnection.openConnection();
            dbConnection.executeQuery("SELECT * FROM student");
            dbConnection.closeConnection();
        }
    */

    }
}
